<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= link_to "وقت نمایش",root_path,class:'navbar-brand' %>
    </div>
    <% if user_signed_in? %>
        <div class="btn-group navbar-left">
            <li class="btn dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="caret"></span> <%= current_user.email %></a>
              <ul class="dropdown-menu" role="menu">
                <%if current_user.has_role?(:admin) %>
                    <%= link_to 'میزکار مدیریت', :admin_panel %>
                    <li class="divider"></li>
                <% end %>
                <%= link_to 'ویرایش مشخصات', edit_user_registration_path %>
                <li class="divider"></li>
                <%= link_to "خروج", destroy_user_session_path, method: :delete  %>
              </ul>
            </li>
        </div>
    <% else %>
    <div class="btn-group navbar-left">
        <%= link_to "ثبت نام", new_user_registration_path, :class => 'btn btn-default navbar-btn',type: 'button'  %>
        <%= link_to "ورود", new_user_session_path, :class => 'btn btn-success navbar-btn',type: 'button'  %>
       </div>
    <% end %>
  </div><!-- /.container-fluid -->
</nav>